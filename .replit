run = "php artisan serve --host=0.0.0.0 --port=8000"
language = "php"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "composer install && php artisan key:generate && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8000"]
