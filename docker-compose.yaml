version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: triangle-pos:latest
    container_name: triangle-pos-app
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - DB_HOST=db
      - DB_PORT=3306
      - DB_DATABASE=${DB_DATABASE:-triangle_pos}
      - DB_USERNAME=${DB_USERNAME:-posuser}
      - DB_PASSWORD=${DB_PASSWORD:-secret}
    volumes:
      - storage-data:/var/www/html/storage
      - ./public:/var/www/html/public
    depends_on:
      db:
        condition: service_healthy
    networks:
      - triangle-network

  db:
    image: mysql:8.0
    container_name: triangle-pos-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootsecret}
      MYSQL_DATABASE: ${DB_DATABASE:-triangle_pos}
      MYSQL_USER: ${DB_USERNAME:-posuser}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - triangle-network

volumes:
  db-data:
    driver: local
  storage-data:
    driver: local

networks:
  triangle-network:
    driver: bridge


